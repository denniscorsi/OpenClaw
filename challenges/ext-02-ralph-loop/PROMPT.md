# Goal

List the 5 most recently modified files in the current directory (non-recursive — top-level only). For each file, write a one-sentence description of what it likely is based on its name and extension.

Write the result to a file called `output.txt` in the current directory.

When you have completed this task and written `output.txt`, include `[DONE]` in your final response.
